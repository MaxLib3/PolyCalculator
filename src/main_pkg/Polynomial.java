package main_pkg;
import java.util.Vector;

public class Polynomial
{
    private Vector<Scalar> poly;

    public static Polynomial build(String input) {
        String[] splited = input.split("\\s+");
        for (String s: splited) {
            
        }
    }
}
